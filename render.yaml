services:
  - type: web
    name: new-project-21-api
    env: node
    rootDir: api
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: API_PORT
        value: 10000
      - key: OPENAI_MODEL
        value: gpt-5.4-mini
