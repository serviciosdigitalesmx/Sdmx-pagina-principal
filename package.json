{
  "name": "sdmx",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"pnpm --dir apps/web dev\" \"pnpm --dir apps/backend-api dev\"",
    "build": "pnpm --dir apps/web build && pnpm --dir apps/backend-api build",
    "lint": "pnpm --dir apps/web lint && pnpm --dir apps/backend-api lint",
    "e2e:selenium": "node scripts/e2e-selenium.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "selenium-webdriver": "^4.31.0"
  }
}
